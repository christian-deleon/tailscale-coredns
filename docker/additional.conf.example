# Route53 Plugin Example Configuration
#
# This example shows how to use the route53 plugin with AWS credentials
#
# Prerequisites:
# 1. AWS credentials configured (via environment variables, IAM roles, or credentials file)
# 2. Route53 hosted zone created
# 3. Proper IAM permissions for Route53 access
#
# Required AWS permissions:
# - route53:ListHostedZones
# - route53:ListResourceRecordSets
# - route53:GetChange
# - route53:ChangeResourceRecordSets (if you want to modify records)

# Example private domain zone
example.private. {
    route53 example.private.:Z0123456789ABCDEF
    fallthrough
    log
    errors
}

# Example with multiple zones
# You can have multiple route53 zones in the same configuration
# another-example.com. {
#     route53 another-example.com.:Z0987654321FEDCBA
#     fallthrough
#     log
#     errors
# }

# Example with AWS region specification (optional)
# If your Route53 zone is in a different region, you can specify it
# myzone.example.com. {
#     route53 myzone.example.com.:Z0123456789ABCDEF {
#         aws_region us-west-2
#     }
#     fallthrough
#     log
#     errors
# }